# Python Port Scanner

Port Scanner is a network tool which is used to scan open ports on any network. This tool uses `socket` python module which scans TCP ports on target host.

## Features 

- Scans single IP's currently.
- Detect open ports.

## Requirements

- Python 3
- Socket module

# How to run
- git clone the files
- Run `python3 scanner.py`
